version: '3.8'
services:
  web:
    image: cr.yandex/${REGISTRY_ID}/web-app:latest
    container_name: web-app
    ports:
      - "80:80"
    environment:
      - DB_HOST=${DB_HOST}
      - DB_USER=${DB_USER}
      - DB_NAME=${DB_NAME}
    restart: unless-stopped